export default function Posts() {
    return (
      <div>
        <h1>Listagem de posts</h1>
        <ul>
          {/* {posts.map((posts) => (
            <li key={posts.id}>{posts.title}</li>
          ))} */}
        </ul>
      </div>
    )
  }